<div class="row">
    <h1>Characters</h1>
    {% for character in characters %}
        <div class="col-md-4">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ character.getName }}</h5>

                </div>
            </div>
        </div>
    {% endfor %}
</div>
